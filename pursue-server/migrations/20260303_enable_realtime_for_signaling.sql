-- Documents use of Supabase Realtime Broadcast + Presence for cross-instance
-- signaling relay on Cloud Run. No schema changes required.
-- Required env vars: SUPABASE_URL, SUPABASE_SECRET_KEY
-- Channel naming: signal:{sessionId}
SELECT 1;
