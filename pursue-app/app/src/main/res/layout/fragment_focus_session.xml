<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@android:color/black">

    <!-- ===== VIDEO AREA: preview overlay OR session grid ===== -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <!-- Session grid (visible after Go Live) -->
        <GridLayout
            android:id="@+id/video_grid"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:columnCount="2"
            android:rowCount="2"
            android:padding="4dp"
            android:background="@android:color/black"
            android:visibility="gone" />

        <!-- Preview overlay (visible before Go Live) -->
        <FrameLayout
            android:id="@+id/preview_overlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/black">

            <!-- Participant count badge (SurfaceViewRenderer added programmatically below it) -->
            <TextView
                android:id="@+id/preview_participant_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom|center_horizontal"
                android:layout_marginBottom="16dp"
                android:padding="8dp"
                android:textColor="@android:color/white"
                android:textAppearance="?attr/textAppearanceBodyMedium"
                android:background="#80000000"
                android:visibility="gone" />

        </FrameLayout>

    </FrameLayout>

    <!-- ===== CAMERA CONTROLS ===== -->
    <LinearLayout
        android:id="@+id/camera_controls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="8dp"
        android:background="@android:color/black">

        <ImageButton
            android:id="@+id/btn_toggle_camera"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginHorizontal="12dp"
            android:src="@drawable/ic_videocam"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/focus_session_toggle_camera"
            app:tint="@android:color/white" />

        <ImageButton
            android:id="@+id/btn_switch_camera"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginHorizontal="12dp"
            android:src="@drawable/ic_switch_camera"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/focus_session_switch_camera"
            app:tint="@android:color/white" />

        <ImageButton
            android:id="@+id/btn_toggle_mic"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginHorizontal="12dp"
            android:src="@drawable/ic_mic_off"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/focus_session_toggle_mic"
            app:tint="@android:color/white" />

        <ImageButton
            android:id="@+id/btn_toggle_speaker"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginHorizontal="12dp"
            android:src="@drawable/ic_hearing"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Speaker"
            app:tint="@android:color/white" />

    </LinearLayout>

    <!-- ===== PREVIEW CONTROLS (visible before Go Live) ===== -->
    <LinearLayout
        android:id="@+id/preview_controls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:gravity="center"
        android:background="@color/background_light"
        android:visibility="visible">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/focus_session_ready_to_join"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textColor="@color/on_surface_variant"
            android:layout_marginBottom="12dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_go_live"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="@string/focus_session_go_live"
            android:textColor="@color/on_primary"
            app:backgroundTint="@color/primary" />

    </LinearLayout>

    <!-- ===== LOBBY CONTROLS (visible in lobby phase, after Go Live) ===== -->
    <LinearLayout
        android:id="@+id/lobby_controls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="@color/background_light"
        android:visibility="gone">

        <!-- Group name -->
        <TextView
            android:id="@+id/lobby_group_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceTitleLarge"
            android:textColor="@color/on_surface"
            android:layout_marginBottom="4dp"
            tools:text="Morning Writers" />

        <!-- Subtitle -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/focus_session_lobby"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textColor="@color/on_surface_variant"
            android:layout_marginBottom="8dp" />

        <!-- People count -->
        <TextView
            android:id="@+id/lobby_people_count"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textColor="@color/on_surface_variant"
            android:layout_marginBottom="12dp"
            tools:text="2 people here" />

        <!-- Duration selector label -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/schedule_slot_duration"
            android:textAppearance="?attr/textAppearanceLabelLarge"
            android:textColor="@color/on_surface"
            android:layout_marginBottom="8dp" />

        <!-- Duration chips -->
        <com.google.android.material.chip.ChipGroup
            android:id="@+id/lobby_duration_chips"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            app:singleSelection="true"
            app:checkedChip="@id/chip_45">

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_25"
                style="@style/Widget.MaterialComponents.Chip.Choice"
                android:layout_width="wrap_content"
                android:layout_height="48dp"
                android:text="@string/focus_session_duration_25" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_45"
                style="@style/Widget.MaterialComponents.Chip.Choice"
                android:layout_width="wrap_content"
                android:layout_height="48dp"
                android:text="@string/focus_session_duration_45"
                android:checked="true" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_60"
                style="@style/Widget.MaterialComponents.Chip.Choice"
                android:layout_width="wrap_content"
                android:layout_height="48dp"
                android:text="@string/focus_session_duration_60" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_90"
                style="@style/Widget.MaterialComponents.Chip.Choice"
                android:layout_width="wrap_content"
                android:layout_height="48dp"
                android:text="@string/focus_session_duration_90"
                android:enabled="false" />

        </com.google.android.material.chip.ChipGroup>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_start_focus"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="@string/focus_session_start_focus"
            android:textColor="@color/on_primary"
            app:backgroundTint="@color/primary"
            android:visibility="gone" />

    </LinearLayout>

    <!-- ===== FOCUS OVERLAY (visible in focus phase) ===== -->
    <LinearLayout
        android:id="@+id/focus_overlay"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="16dp"
        android:background="@color/background_light"
        android:visibility="gone">

        <!-- Phase label -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/focus_session_focus_block"
            android:textAppearance="?attr/textAppearanceTitleLarge"
            android:textColor="@color/on_surface"
            android:layout_marginBottom="8dp" />

        <!-- Countdown timer -->
        <TextView
            android:id="@+id/focus_timer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="48sp"
            android:textStyle="bold"
            android:textColor="@color/on_surface"
            android:fontFamily="monospace"
            android:layout_marginBottom="8dp"
            tools:text="34:12" />

        <!-- Mic muted notice -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/focus_session_mic_muted"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textColor="@color/on_surface_variant"
            android:layout_marginBottom="12dp" />

        <!-- End early (host only) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_end_early"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:text="@string/focus_session_end_early"
            android:textColor="@color/error"
            app:strokeColor="@color/error"
            android:visibility="gone" />

    </LinearLayout>

    <!-- ===== CHIT-CHAT OVERLAY (visible in chit-chat phase) ===== -->
    <LinearLayout
        android:id="@+id/chit_chat_overlay"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp"
        android:background="@color/background_light"
        android:visibility="gone">

        <!-- Header row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="4dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/focus_session_chit_chat"
                android:textAppearance="?attr/textAppearanceTitleLarge"
                android:textColor="@color/on_surface" />

            <TextView
                android:id="@+id/chit_chat_timer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textAppearance="?attr/textAppearanceBodyMedium"
                android:textColor="@color/on_surface_variant"
                android:fontFamily="monospace"
                tools:text="10:00" />

        </LinearLayout>

        <!-- Chat prompt -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/focus_session_chit_chat_prompt"
            android:textAppearance="?attr/textAppearanceBodyMedium"
            android:textStyle="italic"
            android:textColor="@color/on_surface_variant"
            android:layout_marginBottom="4dp" />

        <!-- Chat messages -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/chit_chat_messages"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:layout_marginBottom="4dp" />

        <!-- Message input row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <com.google.android.material.textfield.TextInputLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:hint="@string/focus_session_message_hint"
                style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">

                <com.google.android.material.textfield.TextInputEditText
                    android:id="@+id/chit_chat_message_input"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:maxLines="3"
                    android:imeOptions="actionSend" />

            </com.google.android.material.textfield.TextInputLayout>

            <ImageButton
                android:id="@+id/btn_send_message"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginStart="8dp"
                android:src="@android:drawable/ic_menu_send"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Send" />

        </LinearLayout>

    </LinearLayout>

</LinearLayout>
