<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name="app.getpursue.PursueApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pursue"
        tools:targetApi="31">

        <activity
            android:name="app.getpursue.ui.activities.OnboardingActivity"
            android:exported="false" />

        <activity
            android:name="app.getpursue.ui.activities.MainAppActivity"
            android:exported="false" />

        <activity
            android:name="app.getpursue.ui.activities.OrientationActivity"
            android:exported="false" />

        <activity
            android:name="app.getpursue.ui.activities.GroupDetailActivity"
            android:exported="false"
            android:parentActivityName="app.getpursue.ui.activities.MainAppActivity" />

        <activity
            android:name="app.getpursue.ui.activities.ShareableMilestoneCardActivity"
            android:exported="false"
            android:parentActivityName="app.getpursue.ui.activities.MainAppActivity" />

        <activity
            android:name="app.getpursue.ui.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="getpursue.app" android:pathPrefix="/join/" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="getpursue.app" android:pathPrefix="/challenge/" />
            </intent-filter>
        </activity>

        <!-- Firebase Cloud Messaging Service -->
        <service
            android:name="app.getpursue.data.fcm.PursueFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- FileProvider for sharing files (camera photos, avatar cache) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode_ui" />

        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="${firebase_crashlytics_collection_enabled}" />

        <meta-data
            android:name="google_analytics_collection_enabled"
            android:value="false" />

    </application>

</manifest>
